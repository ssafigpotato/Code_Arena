services:
  db:
    image: mysql:8.0
    container_name: db
    ports:
      - 3306:3306
    env_file: .env
    environment:
      TZ: Asia/Seoul
    restart: always
    networks:
      - backend

networks:
  backend: